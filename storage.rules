rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 商品照片：公開可讀(前台顯示用)，上傳/刪除限後台登入帳號
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
